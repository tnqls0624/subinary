# Dependencies (installed inside the image)
node_modules
**/node_modules

# Build outputs (rebuilt inside the image)
dist
**/dist
.next
**/.next
.turbo
**/.turbo

# VCS
.git

# Environment (secrets — injected via compose env_file, never baked into the image)
.env

# Logs
*.log

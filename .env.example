# --- App ---
NODE_ENV=development
TZ=Asia/Seoul
API_PORT=3001
WORKER_PORT=3002
WEB_PORT=3000

# --- PostgreSQL ---
POSTGRES_HOST=postgres
POSTGRES_PORT=5432
POSTGRES_USER=family
POSTGRES_PASSWORD=family_dev_pw
POSTGRES_DB=family_memory
# 앱이 사용하는 연결 문자열(컨테이너 내부 네트워크 기준)
DATABASE_URL=postgresql://family:family_dev_pw@postgres:5432/family_memory

# --- Redis / BullMQ ---
REDIS_HOST=redis
REDIS_PORT=6379
BULLMQ_PREFIX=fma

# --- Object Storage (MinIO=S3 호환) ---
STORAGE_ENDPOINT=http://minio:9000
STORAGE_REGION=us-east-1
STORAGE_ACCESS_KEY=minioadmin
STORAGE_SECRET_KEY=minioadmin_dev_pw
STORAGE_BUCKET=family-memory
STORAGE_FORCE_PATH_STYLE=true

# --- Web (브라우저에서 API 접근; 호스트 published 포트) ---
NEXT_PUBLIC_API_URL=http://localhost:3001
# API가 허용하는 웹 오리진(CORS). refresh 쿠키 전송을 위해 credentials 허용.
CORS_ORIGIN=http://localhost:3000

# --- AI Providers (Phase 0는 mock) ---
AI_PROVIDER=mock
# OPENAI_API_KEY=
# ANTHROPIC_API_KEY=

# --- Auth (Phase 1) ---
# 개발용 더미 시크릿(16자+). 운영에서는 반드시 교체할 것.
JWT_ACCESS_SECRET=dev_jwt_access_secret_change_me_please
JWT_ACCESS_TTL_SEC=900
JWT_REFRESH_TTL_SEC=2592000

# --- Device (Phase 2) ---
# 장치 Secret at-rest 암호화 키. 정확히 64자 hex(=32바이트) 개발용 더미. 운영에서는 반드시 교체할 것.
DEVICE_SECRET_ENC_KEY=0123456789abcdef0123456789abcdef0123456789abcdef0123456789abcdef
# HMAC 타임스탬프 허용 오차(초)
HMAC_TIMESTAMP_TOLERANCE_SEC=300
# Nonce 재사용 방지 저장 TTL(초)
DEVICE_NONCE_TTL_SEC=600
# mobile-events 요청 본문 최대 바이트(Fastify bodyLimit)
MOBILE_MAX_BODY_BYTES=16384

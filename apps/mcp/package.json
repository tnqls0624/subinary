{
  "name": "@family/mcp",
  "version": "0.1.0",
  "private": true,
  "description": "Family Memory AI — stdio MCP server (Phase 10). Exposes personal memory + finance tools over the existing HTTP API.",
  "bin": {
    "family-memory-mcp": "dist/main.js"
  },
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "typecheck": "tsc --noEmit",
    "lint": "echo noop",
    "start": "node dist/main.js"
  },
  "dependencies": {
    "@family/contracts": "workspace:*",
    "@family/shared": "workspace:*",
    "@modelcontextprotocol/sdk": "^1.12.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^22.10.7",
    "tsup": "^8.3.5",
    "typescript": "^5.9.3"
  }
}
